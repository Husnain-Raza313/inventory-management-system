<div id="flashes" class="d-none">
  <% flash.each do |key, value| %>
    <div class=<%= key.to_s.gsub('alert', 'error').gsub('notice', 'success') %>>
      <em><%= value %></em>
    </div>
  <% end %>
</div>
