<h1> <%= t('edit', param: "Category") %>Category</h1>
<%= render 'form', category: @category %>
<%= link_to t('show-button'), @category, class:"btn btn-primary" %> |
<%= link_to t('edit-button'), category_path, class:"btn btn-dark" %>
